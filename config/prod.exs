import Config

# Production configuration
# It's recommended to use runtime configuration for production
# See config/runtime.exs for runtime configuration

# config :firebase_admin,
#   project_id: System.get_env("FIREBASE_PROJECT_ID"),
#   credentials: System.get_env("FIREBASE_CREDENTIALS_PATH")
