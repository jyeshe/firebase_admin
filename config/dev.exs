import Config

# Development configuration
# Override with your development Firebase project credentials
# config :firebase_admin,
#   project_id: "dev-project-id",
#   credentials: "priv/dev-service-account.json"
